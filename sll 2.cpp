#include<stdio.h>
#include<stdlib.h>
void insert at_end()
{
	int x;
	printf("enter the element");
	scanf("%d",&x);
	new node=(struct node*)malloc(size of(struct node));
	new node _data=x;
	new node_next=NULL;
	for(p=head;p_next!=NULL;p=p_next);
	p_next=new node;
	p=new node;void display()
	{
		if(head==NULL)
		printf("the SLL is empty");
		else
		{
			for(p=head;p!=NULL;p=p_next)
			printf("%d_",p_data);
		}
	}
}
